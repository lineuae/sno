const ms = require('ms');
const Discord = require('discord.js');

module.exports = {
    name: 'clientReady',
    /**
     * 
     * @param {import("../../structures/client")} client 
     */
    run: async (client) => {
        return;
    }
};
